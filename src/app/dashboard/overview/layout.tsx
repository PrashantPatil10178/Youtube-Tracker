/**
 * The starter template drove this page through four parallel-route slots
 * (@sales, @pie_stats, @bar_stats, @area_stats) rendering mock revenue charts.
 * Those slots and their fixtures are deleted; the overview is a normal
 * page.tsx now.
 */
export default function OverviewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
