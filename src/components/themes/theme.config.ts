/**
 * Only one theme ships now — the selector was removed from the header, so this
 * list exists purely to validate the `active_theme` cookie in the root layout.
 */
export const DEFAULT_THEME = 'sarvam';

export const THEMES = [
  {
    name: 'Sarvam',
    value: 'sarvam'
  }
];
